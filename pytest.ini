[pytest]
testpaths = tests
addopts = --tb=short -v
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    integration: marks end-to-end integration tests
    edge_case: marks edge case and exception handling tests
    alerts: marks alerting and healing module tests
